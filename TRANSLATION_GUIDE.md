# دليل استخدام نظام الترجمة

تم إضافة نظام ترجمة متعدد اللغات يدعم العربية والفرنسية والإنجليزية في تطبيق تأجير السيارات.

## المكونات الرئيسية

1. **TranslationService** - خدمة الترجمة المسؤولة عن إدارة اللغات والترجمات
2. **TranslatePipe** - دالة (pipe) لاستخدام الترجمات في القوالب
3. **LanguageSelectorComponent** - مكون لاختيار اللغة

## كيفية الاستخدام

### في قوالب HTML

استخدم دالة الترجمة (translate pipe) مع مفتاح الترجمة:

```html
<!-- مثال: ترجمة نص -->
<span>{{ 'navbar.cars' | translate }}</span>

<!-- مثال: ترجمة نص مع متغيرات -->
<span>{{ 'carDetails.price' | translate }}: {{ car.price | currency }}</span>
```

### في مكونات TypeScript

استخدم خدمة الترجمة مباشرة:

```typescript
import { TranslationService } from '../services/translation.service';

constructor(private translationService: TranslationService) {}

// الحصول على الترجمة الحالية
const translatedText = this.translationService.translate('navbar.cars');

// تغيير اللغة
this.translationService.setLanguage('fr');

// الاشتراك في تغييرات اللغة
this.translationService.currentLanguage$.subscribe(lang => {
  console.log('Current language:', lang);
});
```

## إضافة ترجمات جديدة

لإضافة ترجمات جديدة، قم بتعديل ملف `translation.service.ts` في قسم `getTranslations()`:

```typescript
private getTranslations() {
  return {
    ar: {
      // ترجمات عربية
      'new.key': 'الترجمة العربية الجديدة'
    },
    fr: {
      // ترجمات فرنسية
      'new.key': 'Nouvelle traduction française'
    },
    en: {
      // ترجمات إنجليزية
      'new.key': 'New English translation'
    }
  };
}
```

## اللغات المدعومة

1. العربية (ar) - 🇸🇦
2. الفرنسية (fr) - 🇫🇷
3. الإنجليزية (en) - 🇬🇧

## حفظ تفضيلات اللغة

يتم حفظ اختيار اللغة في التخزين المحلي (localStorage) للمستعرض، وسيتم استعادته عند زيارة التطبيق مرة أخرى.

## ملاحظات هامة

- تأكد من استخدام نفس المفتاح في جميع اللغات
- استخدم مفتاحًا وصفيًا يعبر عن المحتوى (مثال: 'navbar.cars' أفضل من 'cars')
- عند إضافة نص جديد، أضف ترجمته في جميع اللغات المدعومة
